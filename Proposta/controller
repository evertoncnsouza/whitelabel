- IntegracoesController

Recebe um AtomicInteger...{
Atomic Integer - Um int que pode ser atualizado atomicamente. 
Um AtomicIntegeré usado em aplicativos como contadores incrementados atomicamente e
não pode ser usado como um substituto para um Integer. No entanto, essa classe se
estende Number para permitir acesso uniforme por ferramentas e utilitários que lidam
com classes baseadas em números.}
.... para ser utilizado um getAndIncrement é um método embutido em java que aumenta 
o valor fornecido em um e retorna o valor antes da atualização, que é do tipo de 
dados int .

Este controller possui um calculo  para retornar com ou sem restrição;

#############################################################

-PropostaController
Esta Classe recebe bloqueioDocIgualValidator, o compoment executortransacao e 
o service avaliaProposta.

Foi preferível receber esses itens acima via construtor.

No metodo post, Temos um Entity<?>, recebendo um UriComponentsBuilder, pois precisamos
retornar a resposta no header. 

Detalhe para o Unprocessable_entity, que retorna 422. 
#############################################################

-ValidationErrorHandler
